// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  
  firebaseConfig: {
    apiKey: "AIzaSyAgFyqSNteQl0_LZg5Z-F1iuz8gN5RcWns",
    authDomain: "backend-proyecto-79ddd.firebaseapp.com",
    projectId: "backend-proyecto-79ddd",
    storageBucket: "backend-proyecto-79ddd.appspot.com",
    messagingSenderId: "791649650600",
    appId: "1:791649650600:web:bab74295e1369b6167bf9d"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
